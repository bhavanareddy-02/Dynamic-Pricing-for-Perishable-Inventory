# Streamlit Cloud reads this file from the repo root.
# Dashboard-only dependencies. Pipeline-only packages (prophet, statsmodels,
# xgboost) are NOT here — those are needed only for run_pipeline.py which you
# run locally.

streamlit>=1.30
plotly>=5.18
pandas>=2.0
numpy>=1.24,<2.0
scikit-learn>=1.3
lightgbm>=4.0
